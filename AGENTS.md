# Project RTD 에이전트 실행 규칙

## 게임 실행

사용자가 `실행` 또는 `게임 실행`이라고 입력하면 다음 순서로 처리한다.

1. 프로젝트 루트에서 `node server.js`를 실행한다.
2. Codex 내부 브라우저 패널을 사용하지 않는다.
3. 외부 브라우저(사용자 브라우저/브라우저 확장 연결)에서 `http://localhost:3000/`을 연다.
4. 서버가 이미 실행 중이면 중복 실행하지 않고 외부 브라우저에서 주소만 연다.

외부 브라우저 연결이 불가능한 경우에는 게임 서버 실행 상태와 주소를 사용자에게 알리고,
외부 브라우저 확장 연결이 필요하다고 안내한다.

## 배포버전 빌드

배포용 서버는 `npm run start:deploy`로 실행하며 기본 포트는 `3001`이다. 이미 3001 포트가
실행 중이면 중복으로 실행하지 않는다. 배포 서버는 `NODE_ENV=production`으로 동작해야 하며,
개발용 API와 개발 도구를 노출하지 않는다.

- 배포판의 게임 배속 선택지는 `1X`, `2X`, `3X`만 활성화한다. 개발판의 `0.5X`, `4X` 이상 배속은
  배포판에 표시하거나 저장값으로 복원하지 않는다.
- `화면 맞춤` UI는 개발 환경의 레이아웃 점검용이므로 배포판에는 표시하거나 활성화하지 않는다.
- 시작 화면의 DEV MODE 진입·표시·토글, 계산 시뮬레이션 패널(정확도·시간·횟수 설정 포함),
  계산 리포트·누적 데이터 표시는 배포판에서 제거한다.
- 개발자 골드 획득 버튼과 관련 이벤트, DEV MODE 전용 `ESC → 메인 화면` 이동, 개발자 단축키,
  테스트 버튼, 디버그 표시 및 DEV 데이터 수집은 배포판에서 비활성화한다.
- 배포 환경에서는 DEV 전용 API를 모두 차단한다. 대상에는 시뮬레이션 리포트, 계산 시뮬레이션의
  실행·저장·동기화, DEV 데이터 기록이 포함된다.
- 브라우저 저장소에 DEV 데이터나 계산 기록이 남아 있어도 배포판은 이를 읽거나 화면·게임 흐름에
  반영하지 않는다.
- 배포판은 게임 시작, 뽑기·자동 합성, 전투·보상·게임 오버의 일반 게임 흐름만 제공한다.
- 일반 전투용 DPS 기능은 배포판에서 활성화한다. 이는 DEV MODE·계산 시뮬레이션 기능과 별개다.
- 중거리 예약 배치 계산은 유지하되, 배포판에서는 중거리 예약 범례·예약 슬롯 강조 표시와
  `ZONE 1`·`ZONE 2`·`ZONE 3` 경계 및 텍스트 UI를 표시하지 않는다.
- 배포 변경 후에는 개발 서버(3000)와 분리된 3001 배포 테스트 서버에서 시작, 전투, 저장,
  새로고침, 게임 오버 및 리소스 로딩을 점검한다.
- 위 제한은 배포 런타임 설정을 기준으로 적용한다. 개발 환경의 테스트 기능과 UI는 별도 요청이
  없는 한 유지한다.

## 게임 실행 & 데이터 툴 실행

단축어 `데이터 툴 실행`을 입력하면 외부 브라우저에서 데이터 툴을 실행한다.

## 라운드 구역 명칭

8라운드 단위 진행 구성을 다음 구역 명칭으로 구분한다.

- 1구역: R1~R8
- 2구역: R9~R16
- 3구역: R17~R24
- 4구역: R25~R32
- 5구역: R33~R40
- 6구역: R41~R48
- 7구역: R49~R52 (8의 배수가 아닌 특별 구성)

## 몬스터 HP·골드 조정 기준

몬스터의 생명력(HP)과 획득 골드에 대한 전반적인 비율 조정은 항상 현재 프로젝트에
적용된 값을 기준값으로 사용한다. 과거 원본값이나 임의의 고정값을 기준으로 재계산하지 않는다.

- 고정값 조정: 조정 직전의 현재 적용값을 기준으로 증감한다.
- 비율(%) 조정: 조정 직전의 현재 적용값에 비율을 곱해 계산한다.
- 밸런스 조정으로 데이터 값을 수정해 적용한 경우, 수정 후 적용된 값을 이후 조정의 기준값으로 사용한다.
- HP·골드 중 한 항목만 조정할 때 다른 항목의 현재 적용값은 유지한다.
- 조정 전후 기준값과 계산 결과를 검증하고, 같은 조정을 반복 적용해 값이 누적 왜곡되지 않도록 한다.
- 별도의 요청이 없는 경우 기존 라운드 배율 보정은 변경하지 않는다.
- 모든 몬스터의 킬 골드는 일반·보스 구분 없이 소수점 1자리까지만 적용한다. 게임 화면과 연출에서는 정수로 표시하되, 내부 경제 누적값은 이 소수점 1자리 값을 사용한다.

## 라운드 배율 정의

라운드 배율은 다음 공식으로 정의한다.

```text
몬스터 생명력 배율 = 1 + (현재 라운드 × 0.0115)
골드 획득 배율 = 1 + (현재 라운드 × 0.005)
```

각 배율의 계수는 라운드 배율 값이며, 적용 대상은 다음 항목을 서로 독립적으로 구분한다.

- 몬스터 생명력 배율 값
- 골드 획득 배율 값

별도의 요청이 없으면 각 항목의 기존 배율 값과 적용 방식을 변경하지 않는다.
